version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: gemma-hub-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  app:
    build: .
    container_name: gemma-hub-app
    ports:
      - "5002:5002"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=sqlite:////app/data/gemma_hub.db
    volumes:
      - ./uploads:/app/uploads
      - db_data:/app/data
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  redis_data:
  db_data:
